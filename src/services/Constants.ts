//const base = 'https://tixid-app.herokuapp.com/';
const base = 'http://localhost:5000/';
const Const = {
  URL: {
    BASE: base,
    WS: base + 'ws',
    GAME: base + 'game/',
    GAMES: base + 'games/',
    LOGIN: base + 'login',
    LOGOUT: base + 'logout'
  },

  VERSION: '1.0'
}

export default Const;
